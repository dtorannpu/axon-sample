spring:
  application:
    name: axon-sample
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
axon:
  axonserver:
    enabled: false
  serializer:
    general: jackson
    events: jackson
    messages: jackson
